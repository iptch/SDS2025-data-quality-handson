create table bike_rental
(
    id         integer,
    dteday     text,
    season     text,
    year       integer,
    mnth       integer,
    hour       integer,
    holiday    integer,
    weekday    text,
    workingday integer,
    weather    text,
    temp       double,
    felt_temp  text,
    humidity   text,
    windspeed  text,
    casual     text,
    registered text,
    total      text
);